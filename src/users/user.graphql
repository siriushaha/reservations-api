type User {
    id: ID!
    name: String!
    email: String!
    status: Status!
    role: Role!
    reservations: [Reservation!]!
    createdAt: DateTime!
    updatedAt: DateTime!
}

type Query {
    users: [User!]!
}
