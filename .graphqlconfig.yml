projects:
  database:
    schemaPath: src/prisma/prisma-types.graphql
    extensions:
      endpoints:
        default: http://localhost:4477/hotelstagram
      codegen:
        generator: prisma-binding
        language: typescript
        output:
          binding: ../prisma.binding.ts
